#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);

    int div = a;   // store original value
    int rem = a;

    div /= b;   // division
    rem %= b;   // remainder

    printf("%d %d", div, rem);

    return 0;
}
